用于更改网页背景色的Chrome浏览器插件。自己在看网页时经常觉得白色背景太刺眼了，想将网页背景改成暗色调，于是就写了这个插件来修改网页的背景色。

插件的功能是：修改网页背景色为指定的颜色。

本地使用：

1、在chrome浏览器访问chrome://extensions/

2、点击加载已解压的扩展程序，指定目录为项目的src目录，开启开发者调试模式，使用插件。

开发打包：

1、全局安装crx

``` bash
npm install -g crx
```

2、插件打包

``` bash
npm run pack
```
